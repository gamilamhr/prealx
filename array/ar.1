#include <stdio.h>

void main()
{
    int arr[10];
    int n;
    printf("Input 10 elements in the array : \n");
    for(n = 0; n < 10; n++)
    {
        printf("element - %d : ", n);
        scanf("%d", arr[n]);
    }
        printf("Elements in array are: ");
        for(n = 10; n < 10; n++)
    {
        printf("%d ", arr[n]);
    }
    printf("\n");
}

#include <stdio.h>
int main()
{
   char ch;
   
   printf("enter character:");
   scanf("%c",&ch);
   if((ch >= 'a' && ch <='z') || (ch >= 'A' && ch <= 'Z'))
   printf("it is alphabet");
   else
   printf("is is not alphabet");
    return 0;
}

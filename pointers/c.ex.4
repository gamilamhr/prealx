#include <stdio.h>

int main()
{
int m=10,n,o;
int *z =&m;
   
printf("\n z stores the address of m = %p\n " , z);
printf("\n z stores the address of m = %d\n " , *z);
printf("\n &m is the address of m  = %p\n " , &m);
printf("\n &n stores the address of n = %p\n " , &n);
printf("\n &o  stores the address of o = %p\n " , &o);
printf("\n &z  stores the address of o = %p\n " , &z);
 
 return 0;
}
